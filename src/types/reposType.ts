export type reposType = {
    id?: number,
    name?: string,
    html_url?: string,
    description?: string,
    created_at?: string,
    updated_at?: any,
    pushed_at?: string,
    stargazers_count?: number,
    watchers_count?: number,
    language?: string,
}